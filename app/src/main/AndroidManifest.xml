<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carplanner">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarPlanner">
        <activity android:name=".AddingPt"></activity>
        <activity android:name=".AddingOc" />
        <activity android:name=".AddingExpensens" />
        <activity android:name=".AddingRepairs" />
        <activity android:name=".AddingFuel" />
        <activity android:name=".AddingActivity" />
        <activity android:name=".FullWidget" />
        <activity android:name=".About" />
        <activity android:name=".SingleItemActivity"/>

        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:screenOrientation="fullSensor" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.CarPlanner.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>